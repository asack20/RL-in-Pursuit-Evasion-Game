
class Pursuer:

    # Class Attribute
    species = 'mammal'

    # Initializer / Instance Attributes
    def __init__(self, m, init_pose, fov):
        self.robot = Robot(m, init_pose, fov)
        self.age = age

    def search(arg):
        pass

    def hasTagged():
        # sense
        # if within 1 m, pursuer has tagged evader
        pass
